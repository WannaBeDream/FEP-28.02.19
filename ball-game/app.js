function BallGame(el, height, width){

}

const game = new BallGame(
                        document.getElementById('container'),
                        400,
                        400
                    );

game.init();