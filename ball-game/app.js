function BallGame(el, height, widtg){

}

const game = new BallGame(
                        document.getElementById('container'),
                        400,
                        400
                    );

game.init();